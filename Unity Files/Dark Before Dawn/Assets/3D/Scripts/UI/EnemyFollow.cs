using UnityEngine;
using System.Collections;

public class EnemyFollow : MonoBehaviour {

	/*public Transform target;
	public float smoothing = 5f;

	Vector3 offset;

	void Start()
	{
		offset = transform.position - target.position;
	}

	void FixedUpdate()
	{
		Vector3 targetEnemyPos = target.position + offset;
		transform.position = Vector3.Lerp (transform.position, targetEnemyPos, smoothing * Time.deltaTime);
	}*/
}
